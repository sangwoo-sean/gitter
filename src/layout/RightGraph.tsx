import styled from "styled-components";

export default function RightGraph() {
  return <Styled>graph here</Styled>;
}

const Styled = styled.div`
  flex: 1;
  display: flex;
  justify-content: center;
  align-items: flex-end;
  padding: 2rem;
`;
